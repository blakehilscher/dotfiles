#!/usr/bin/env ruby
# encoding: UTF-8

gallery_dir = ARGV[0] || 'gallerize'

dir = File.basename(File.expand_path('.'))

puts "chdir #{gallery_dir}"
Dir.chdir(gallery_dir) do |f|
  command = "rsync -avz . www@hilscher.ca:/var/www/photos.hilscher.ca/#{dir}"
  puts command
  system(command)
end
